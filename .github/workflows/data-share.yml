name: Data Share
run-name: ${{ github.actor }} is testing out data share 🚀
on: [push]
jobs:
  Same-Job:
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        run: |
          expr 1 + 1 > output.log
      - shell: bash
        run: |
          cat output.log
  Same-Workflow:
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        run: cat output.log
